
function addItem() {
    const item = prompt("New item:");
    if (item){
        const li = document.createElement("li");
        li.textContent = item;
        document.getElementById("list").appendChild(il);
    }
}

function removeItem() {
        const li = document.createElement("li");
        const last = FileList.lastElementChild;
        if (last) FileList.removeChild
}
